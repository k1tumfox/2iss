const {assert} = require('chai');


describe('fetchMyIP', () => {
  it('parse IP using JSON and passes it through callback as 2nd arg', () => {

  });

  it('passes error to callback if it occurs when req\'ing IP data', () => {

  });

});